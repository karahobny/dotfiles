#!/bin/sh
. "${HOME}/.cache/wal/colors.sh"
#FONT="lime.se:size=8:antialias=false:hinting=full"
FONT="scientifica:size=7"

dmenu_run -fn "$FONT" -nb "$color0" -nf "$color15"\
          -sb "$color1" -sf "$color15" $@
